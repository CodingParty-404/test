<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.BoardMapper">
	
	<!-- SQL Pregment -->
	<sql id="search">
		<foreach collection="typeArr" item="type"  separator="OR" open="AND (" close=")">
			<if test='type.equals("t")' >
				title like concat('%',#{keyword},'%')
			</if>
			<if test='type.equals("c")'>
				content like concat('%',#{keyword},'%')
			</if>
			<if test='type.equals("w")'>
				writer like concat('%',#{keyword},'%')
			</if>
		</foreach>
	</sql>	
	
	<!-- SQL  -->
	<select id="getList" resultType="org.zerock.domain.BoardVO" parameterType="org.zerock.dto.PageDTO">
		select * from tbl_board where bno>0 order by bno desc limit #{skip},#{amount}
	</select>
	<select id="select" resultType="org.zerock.domain.BoardVO">
		select * from tbl_board where bno = #{bno}
	</select>
	
	<select id="getFindList" resultType="org.zerock.domain.BoardVO" parameterType="org.zerock.dto.PageDTO">
		select * from tbl_board
		where bno > 0
		<include refid="search"></include>
		 order by bno desc limit #{skip},#{amount}
	</select>
	
	<select id="getTotal" resultType="int" parameterType="org.zerock.dto.PageDTO">
		select count(bno) from tbl_board
		where bno > 0
		<include refid="search"></include>
		
	</select>
	
	<insert id="insert" parameterType="org.zerock.domain.BoardVO">
		insert into tbl_board (title,content,writer) values(#{title},#{content},#{writer})
	</insert>
	
	<delete id="delete">
		delete from tbl_board where bno = #{bno}
	</delete>
	
	<update id="update">
		update tbl_board set title=#{title}, content=#{content}, moddate=now() where bno =#{bno}
	</update>
	
</mapper>